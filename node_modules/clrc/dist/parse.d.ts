import { LrcLine, MetadataLine, LyricLine, ParseOptions } from './constants';
/**
 * parse lrc string
 * @author mebtte<hi@mebtte.com>
 */
declare function parse<MetadataKey extends string>(lrc: string, { sortByStartTime, trimStart, trimEnd, }?: ParseOptions): {
    metadatas: MetadataLine<MetadataKey>[];
    metadata: { [key in MetadataKey]?: string; };
    lyrics: LyricLine[];
    invalidLines: LrcLine[];
};
export default parse;
